{
  /*"use client";

import { motion } from "framer-motion";
import Image from "next/image";

const connections = [
  {
    name: "bb",
    link: "",
    image: "/avatar.png", // ✅ replace with actual image path if dynamic
    description: "Frontend engineer who builds beautiful personal websites",
  },
  {
    name: "kiran",
    link: "",
    image: "/avatar.png",
    description: "Developer/designer focused on aesthetic & performance",
  },
];

export const ConnectionsBento = () => {
  return (
    <motion.div
      initial={{ opacity: 0, y: 24 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.6 }}
      className="w-full border border-white/[0.1] rounded-2xl bg-white/5 backdrop-blur-md p-4 md:p-8 space-y-8"
    >
      {connections.map((connection, idx) => (
        <div
          key={idx}
          className="flex items-center space-x-4 hover:bg-white/10 p-3 rounded-xl transition"
        >
          <Image
            src={connection.image}
            alt={connection.name}
            width={48}
            height={48}
            className="rounded-full border border-white/10"
          />
          <div className="flex flex-col">
            <a
              href={connection.link}
              target="_blank"
              rel="noopener noreferrer"
              className="text-white font-medium hover:underline"
            >
              {connection.name}
            </a>
            <p className="text-sm text-white/70">{connection.description}</p>
          </div>
        </div>
      ))}
    </motion.div>
  );
};
*/
}
