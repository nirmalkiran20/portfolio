// app/components/FeaturedBlogCard.tsx
{
  /*import Link from "next/link";
import clsx from "clsx";

type FeaturedBlogCardProps = {
  slug: string;
  imageName?: string; // <-- Make imageName optional
  title: string;
  summary: string;
  className?: string;
};

export function FeaturedBlogCard({
  slug,
  imageName,
  title,
  summary,
  className,
}: FeaturedBlogCardProps) {
  return (
    <article className={clsx("bg-surface rounded-lg p-4", className)}>
      <Link href={`/blog/${slug}`}>
        {/* Only show image if imageName is provided 
        {!imageName && (
          <div className="mb-3 flex h-40 w-full items-center justify-center rounded-lg bg-gray-100">
            <span className="text-gray-500">No image</span>
          </div>
        )}

        <h3 className="mb-1 text-lg font-medium text-text-primary">{title}</h3>
        <p className="text-text-secondary">{summary}</p>
      </Link>
    </article>
  );
}
*/
}
